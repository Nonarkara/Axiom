{
  "name": "axiom-landing",
  "version": "2.7.0",
  "description": "Axiom AI - Landing Page",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "PORT=3000 node server.mjs",
    "export:evidence": "node scripts/export-evidence-snapshot.mjs",
    "check": "node --check server.mjs && node --check public/app.js && node --check public/admin/app.js && node --check scripts/export-evidence-snapshot.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "playwright": "^1.61.1"
  }
}
